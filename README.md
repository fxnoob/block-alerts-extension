# Block Alerts chrome extension

### Overrides window.alerts to console.log on web pages.

## Development

```shell
yarn build
```

> `dist` directory will be generated, drag and drop this directory into `brave://extensions/` and reload webpages.
